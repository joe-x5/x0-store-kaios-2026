<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">



<script src="/kaiads.v5.min.js"></script>


<script src="https://joe-x5.github.io/kaios/ban-country.js"></script>



    <title>X0 Store KaiOS</title>

 <meta property="og:image" content="//x0.rf.gd/a/ai-image-genrator/gen.php?prompt=X0-Store-app-games-tools-ai&width=600&height=300" />

  <link rel="icon" href="//x0.rf.gd/a/ai-image-genrator/gen.php?prompt=X0-Store-App&width=128&height=128" />


<meta content='ca-pub-1786122365693674' name='google-adsense-account'/>
  
    

  <meta content='yHxwPbEBzFfMooOWanGYYiIQyCu_4Ai9bGxLQ4SP07U' name='google-site-verification'/>
  

 
  <meta content='X0 All : Tools App | Alternatives of Chat Max All' property='og:title'/>
  
  <meta content='X0 All lets you connect with friends through our tools, chats, create group chats, and join global chats. ' property='og:description'/>

  <meta content='KaiOS No.1 Tools App with 50K+ downloads and 50K+ users in just 3 months! X0 All lets you connect with friends through private chats, create group chats, and join global chats. No phone number required—just start chatting instantly! Send images and express yourself freely. Make new friends, share your feelings, and enjoy real-time conversations. Exciting new features, including **voice and video chat**, are coming soon. Stay connected and experience seamless chatting like never before!' name='description'/>




<style>
.cursor-enabled {
    cursor: default; /* Enables mouse cursor */
}
</style>

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-containerserif;
            background: #f5f5f5;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
          
           
        }


                 .container {
            width: 100%; 
            background-color: #fff;
            border-radius: 12px;
            box-shadow: 0 3px 10px rgba(0, 0, 0, 0.1);
            padding: 5px;
            text-align: center;
            margin-bottom: 20px;

        }

        #liveTime {
            font-size: 12px;
            font-weight: bold;
            color: #007aff;
            margin-bottom: 10px;
        }

        #userIP {

            font-size: 10px;
            font-weight: bold;
            color: #333;
            text-align: center;
            margin-bottom: 10px;
        }

        h1 {
            color: #333;
            margin-bottom: 15px;
            font-size: 22px;
        }

        
        .version-buttons {
            display: grid;
            grid-template-columns: repeat(1, 1fr);
            gap: 10px;
            margin: 5px;
         
       }


        .version-buttons button {
            margin: 5px;
            
            padding: 10px;
            font-size: 1em;
            background: #00a884;
            color: #fff;
            border-radius: 10px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            box-shadow: 0 1px 4px rgba(0, 0, 0, 0.2);
            text-align: center;
            font-weight: bold;
            display: flex;
            align-items: center;
            justify-content: center;
        }

                .version-buttons button:hover,
        .version-buttons button:focus {
            background: #000000;
            border: 2px solid red;
            outline: 1px;
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.3);
        }
         

        .footer {
            margin-top: 15px;
            font-size: 14px;
            color: #ccc;
        }

        .confirm-modal {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.7);
            display: none;
            align-items: center;
            justify-content: center;
        }

        .confirm-modal .modal-content {
            background: white;
            padding: 20px;
            border-radius: 10px;
            text-align: center;
        }

        .confirm-modal button {
            margin: 10px;
            padding: 10px 20px;
            font-size: 16px;
            border: none;
            background-color: #007aff;
            color: white;
            border-radius: 8px;
            cursor: pointer;
        }

        .confirm-modal button:hover {
            background-color: #0051a8;
        }
    </style>
</head>
<body>


   
    <div class="container">
    

 <div style="
  margin-top: 5px;
  border: 2px solid black;
  border-radius: 20px;
  box-shadow: 4px 4px 10px rgba(0, 0, 0, 0.5);
  padding: 20px;
  background-color: white;
">

<img src="icon.png" alt="X0 All" style="width: 120px; height: 120px; border: 2px solid red; border-radius:50%;"/>

<p style="font-size: 18px; font-weight: bold; text-align: center;">X0 All KaiOS</p>

        <br>
        <marquee behavior="scroll" direction="left" scrollamount="5" style="
  color: #0ff;
  font-size: 24px;
  font-weight: bold;
  text-shadow: 2px 2px 5px #00f, -2px -2px 5px #0ff;
  transform: rotateX(10deg) rotateY(5deg);
  display: inline-block;
  animation: glow 2s infinite alternate;
">
 ( Store ) Free Tools , Apps , Chatting, Games , Etc.
</marquee>

<style>
@keyframes glow {
  from {
    text-shadow: 2px 2px 5px #00f, -2px -2px 5px #0ff;
  }
  to {
    text-shadow: 4px 4px 15px #0ff, -4px -4px 15px #00f;
  }
}
</style>


      </div>


            <br>
    <div style="
  margin-top: 5px;
  border: 2px solid pink;
  border-radius: 20px;
  box-shadow: 4px 4px 10px rgba(0, 0, 0, 0.5);
  padding: 20px;
  background-color: white;
overflow: auto;
">
            <div id="liveTime">Loading time...</div>
    
    <div id="userIP">Fetching IP Address...</div>

           <br>
        </div>

          
    <div style="
  margin-top: 5px;
  border: 2px solid green;
  border-radius: 20px;
  box-shadow: 4px 4px 10px rgba(0, 0, 0, 0.5);
  padding: 2px;
  background-color: white;
">
        <h1>X0 Store KaiOS</h1>
        <div class="version-buttons">


<button onclick="openVersion('education/')">
                    <img src="education/icon.png" alt="Education" style="width: 20px; margin-right: 8px;"/> Education
                </button>

                <button onclick="openVersion('social/')">
                    <img src="social/icon.png" style="width: 20px; margin-right: 8px;"/> Social
                </button>

      <button onclick="openVersion('games/')">  
<img src="games/icon.png" style="width: 20px; margin-right: 8px;"/>Games </button>


                <button onclick="openVersion('tools/')">
                    <img src="tools/icon.png" style="width: 20px; margin-right: 8px;"/> Tools
                </button>

<button onclick="openVersion('multimedia/')">
                    <img src="multimedia/icon.png" style="width: 20px; margin-right: 8px;"/> Multimedia 
                </button>



                         <button onclick="openVersion('ai/')">
                    <img src="//x0.rf.gd/a/ai-image-genrator/gen.php?prompt=ai%20icon" style="width: 20px; margin-right: 8px;"/> Ai </button>

<button onclick="openVersion('utility/')">
                    <img src="//x0.rf.gd/a/ai-image-genrator/gen.php?prompt=utility+icon+png" style="width: 20px; margin-right: 8px;"/> Utility 
                </button>




<button onclick="location.reload()">
<img src="//x0.rf.gd/a/ai-image-genrator/gen.php?prompt=refresh%20icon" style="width: 20px; margin-right: 8px;"/>🔄 Refresh</button>



            <?php
            // Comment marker for auto-generated buttons
            // Load apps from JSON
            $appsFile = 'apps.json';
            if (file_exists($appsFile)) {
                $apps = json_decode(file_get_contents($appsFile), true);
                foreach ($apps as $app) {
                    echo '<button onclick="openVersion(\'' . htmlspecialchars($app['url']) . '\')">';
                    echo '<img src="' . htmlspecialchars($app['icon']) . '" alt="' . htmlspecialchars($app['name']) . '"';
                    echo ' style="width: 24px; height: 24px; margin-right: 12px; object-fit: contain;"/>';
                    echo ' ' . htmlspecialchars($app['name']);
                    echo '</button>';
                }
            }
            ?>



            <?php
            // Comment marker for auto-generated buttons
            // Load apps from JSON
            $appsFile = 'https://joe-x5.github.io/x0-store-kaios/apps.json';
            if (file_exists($appsFile)) {
                $apps = json_decode(file_get_contents($appsFile), true);
                foreach ($apps as $app) {
                    echo '<button onclick="openVersion(\'' . htmlspecialchars($app['url']) . '\')">';
                    echo '<img src="' . htmlspecialchars($app['icon']) . '" alt="' . htmlspecialchars($app['name']) . '"';
                    echo ' style="width: 24px; height: 24px; margin-right: 12px; object-fit: contain;"/>';
                    echo ' ' . htmlspecialchars($app['name']);
                    echo '</button>';
                }
            }
            ?>

                </div></div>
           

<script>
  function openVersion(url) {
    window.open(url, '_blank');
  }
</script>

<br>

        
        <div id="ad-container" style="border: none;width: 100%; margin: 0 auto; position: relative; visibility: visible; background-color: transparent; overflow: auto;">

 <iframe data-google-container-id="a!2" data-load-complete="true" frameborder="0" height="90" id="ad-iframe" marginheight="0" marginwidth="0" name="ad-iframe" sandbox="allow-forms allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-top-navigation-by-user-activation" scrolling="yes" src="https://googleads.g.doubleclick.net/pagead/ads?client=ca-pub-1786122365693674&amp;output=html&amp;adk=2062069824&amp;adf=3025194257&amp;lmt=1705043566&amp;w=728&amp;rafmt=9&amp;format=728x90&amp;url=https://tool-max.blogspot.com/&amp;host=ca-host-pub-1556223355139109&amp;" style="border: 0; width: 100%;"></iframe>

<script type='text/javascript'> 
                google_ad_client = "pub-1786122365693674";
                google_ad_host = "pub-1556223355139109"; 
                google_ad_width = 200; 
                google_ad_height = 250; 
                google_ad_type = "text_image"; 
                google_color_border = "FFFFFF"; 
                google_color_bg = "ffffff"; 
                google_color_link = "0000ff"; 
                google_color_text = "000000"; 
                google_color_url = "008000"; 
                google_page_url = "https://tool-max.blogspot.com/"; 
                </script>
                
                
                <script src='https://pagead2.googlesyndication.com/pagead/show_ads.js' type='text/javascript'></script>
                


</div>
          
<?php
// total-visits.php
// ------------------ TOTAL VISITS ------------------
$totalVisitsFile = 'total-visits.json';
$totalVisits = file_exists($totalVisitsFile)
    ? json_decode(file_get_contents($totalVisitsFile), true)['totalVisits'] + 1
    : 1;
file_put_contents($totalVisitsFile, json_encode(['totalVisits' => $totalVisits]));

// ------------------ DAILY VISITS ------------------
$dailyVisitsFile = 'daily-visits.json';
$dailyLogFile    = 'daily-visits.log';

$dailyVisitsData = file_exists($dailyVisitsFile)
    ? json_decode(file_get_contents($dailyVisitsFile), true)
    : [];

$today = date('Y-m-d');
$ip    = $_SERVER['REMOTE_ADDR'];

// increment today / this IP
if (!isset($dailyVisitsData[$today])) $dailyVisitsData[$today] = [];
$dailyVisitsData[$today][$ip] = ($dailyVisitsData[$today][$ip] ?? 0) + 1;
krsort($dailyVisitsData);                     // newest day first
file_put_contents($dailyVisitsFile, json_encode($dailyVisitsData, JSON_PRETTY_PRINT));

// ------------------ WEEKLY VISITS ------------------
$weeklyVisitsFile = 'weekly-visits.json';
$weeklyLogFile    = 'weekly-visits.log';

$weeklyVisitsData = file_exists($weeklyVisitsFile)
    ? json_decode(file_get_contents($weeklyVisitsFile), true)
    : [];

$week = date('Y-W');   // e.g. 2025-45
if (!isset($weeklyVisitsData[$week])) $weeklyVisitsData[$week] = [];
$weeklyVisitsData[$week][$ip] = ($weeklyVisitsData[$week][$ip] ?? 0) + 1;
krsort($weeklyVisitsData);                    // newest week first
file_put_contents($weeklyVisitsFile, json_encode($weeklyVisitsData, JSON_PRETTY_PRINT));

// ------------------ MONTHLY VISITS ------------------
$monthlyVisitsFile = 'monthly-visits.json';
$monthlyLogFile    = 'monthly-visits.log';

$monthlyVisitsData = file_exists($monthlyVisitsFile)
    ? json_decode(file_get_contents($monthlyVisitsFile), true)
    : [];

$month = date('Y-m');  // e.g. 2025-11
if (!isset($monthlyVisitsData[$month])) $monthlyVisitsData[$month] = [];
$monthlyVisitsData[$month][$ip] = ($monthlyVisitsData[$month][$ip] ?? 0) + 1;
krsort($monthlyVisitsData);                   // newest month first
file_put_contents($monthlyVisitsFile, json_encode($monthlyVisitsData, JSON_PRETTY_PRINT));

// ------------------ HUMAN‑READABLE LOGS ------------------
function buildLog($data, $label) {
    $out = "";
    foreach ($data as $key => $ips) {
        $out .= "$label: $key\n";
        foreach ($ips as $ip => $cnt) $out .= "$ip : $cnt\n";
        $out .= "\n";
    }
    return $out;
}

file_put_contents($dailyLogFile,   buildLog($dailyVisitsData,   "Day"));
file_put_contents($weeklyLogFile,  buildLog($weeklyVisitsData,  "Week"));
file_put_contents($monthlyLogFile, buildLog($monthlyVisitsData, "Month"));
?>



<div style="text-align: center;">
  <style>
    @keyframes pulse {
      0% {
        transform: scale(1);
      }
      50% {
        transform: scale(1.1);
      }
      100% {
        transform: scale(1);
      }
    }
    .counter {
      font-size: 24px;
      font-weight: bold;
      color: #333;
      background-color: #f0f0f0;
      padding: 10px;
      border-radius: 5px;
      display: inline-block;
      animation: pulse 2s infinite;
      margin: 10px;
    }
    .counter span {
      font-size: 36px;
      color: #4CAF50;
    }
  </style>
  <p class="counter">Total Views : <span><?php echo $totalVisits; ?></span></p>
  <p class="counter">Today's Users : <span><?php echo isset($dailyVisitsData[$today]) ? count($dailyVisitsData[$today]) : 0; ?></span></p>
</div>



<div style="
  margin-top: 5px;
 font-weight: bold;
  border: 2px solid brown;
  border-radius: 20px;
  box-shadow: 4px 4px 10px rgba(0, 0, 0, 0.5);
  padding: 2px;
  background-color: white;
" class="footer">© <span id="currentYear"></span> Tool Max Pro X</div>
        
    </div>

    <div id="exitModal" class="confirm-modal">
        <div class="modal-content">
            <p>Are you sure you want to exit?</p>
            <button onclick="exitApp(true)">Yes</button>
            <button onclick="exitApp(false)">No</button>
        </div>
    

</div>

    <script>
        function openVersion(url) {
            window.location.href = url;
        }

        document.getElementById("currentYear").innerText = new Date().getFullYear();

        function updateTime() {
            const now = new Date();
            const hours = now.getHours().toString().padStart(2, '0');
            const minutes = now.getMinutes().toString().padStart(2, '0');
            const seconds = now.getSeconds().toString().padStart(2, '0');
            document.getElementById("liveTime").innerText = `⏰ ${hours}:${minutes}:${seconds}`;
        }
        setInterval(updateTime, 1000);
        updateTime();

        document.addEventListener('keydown', function(event) {
            let buttons = document.querySelectorAll(".version-buttons button");
            let focusedElement = document.activeElement;

            if (event.key === "ArrowDown") {
                for (let i = 0; i < buttons.length; i++) {
                    if (buttons[i] === focusedElement && i < buttons.length - 1) {
                        buttons[i + 1].focus();
                        break;
                    }
                }
            } else if (event.key === "ArrowUp") {
                for (let i = 0; i < buttons.length; i++) {
                    if (buttons[i] === focusedElement && i > 0) {
                        buttons[i - 1].focus();
                        break;
                    }
                }
            } else if (event.key === "Enter") {
                focusedElement.click();
            }

            if (event.key === "none") {
                showExitConfirmation();
            }
        });

        window.onload = function() {
            document.querySelector(".version-buttons button").focus();
            fetchUserIP();
        };

        let backPressTimeout;
        function showExitConfirmation() {
            clearTimeout(backPressTimeout);
            backPressTimeout = setTimeout(function() {
                document.getElementById('exitModal').style.display = 'flex';
            }, 1000);
        }

        function exitApp(confirmExit) {
            if (confirmExit) {
                window.close();
            } else {
                document.getElementById('exitModal').style.display = 'none';
            }
        }

        function fetchUserIP() {
            fetch("https://api64.ipify.org?format=json")
                .then(response => response.json())
                .then(data => {
                    document.getElementById("userIP").innerText = `🌍 Your IP: ${data.ip}`;
                })
                .catch(() => {
                    document.getElementById("userIP").innerText = "⚠️ Unable to fetch IP";
                });
        }
   
 
    </script>
    
  
  
  <script>
document.addEventListener("keydown", function (event) {
    if (event.repeat) return; // Avoid multiple triggers

    // Track long press state
    const longPressTimeout = 5000; // 5 seconds
    let longPressTimer;

    // Refresh Page (Long Press 1)
    if (event.key === "1") {
        longPressTimer = setTimeout(() => {
            alert("🔄 Refreshing Page...");
            location.reload(); // Refresh the page
        }, longPressTimeout);
    }

    // Enable/Disable Mouse Cursor (Long Press 0)
    if (event.key === "0") {
        longPressTimer = setTimeout(() => {
            const body = document.body;
            if (body.style.cursor === "none") {
                body.style.cursor = "default";
                alert("🖱️ Mouse Cursor Enabled");
            } else {
                body.style.cursor = "none";
                alert("🖱️ Mouse Cursor Disabled");
            }
        }, longPressTimeout);
    }

    // Toggle Fullscreen Mode (Long Press #)
    if (event.key === "#") {
        longPressTimer = setTimeout(() => {
            if (!document.fullscreenElement) {
                document.documentElement.requestFullscreen();
                alert("📺 Fullscreen Mode Enabled");
            } else {
                document.exitFullscreen();
                alert("📺 Fullscreen Mode Disabled");
            }
        }, longPressTimeout);
    }

    // Exit App (Long Press *)
    if (event.key === "*") {
        longPressTimer = setTimeout(() => {
            alert("🚪 Exiting App...");
            window.close(); // Attempts to close the app (may not work in some environments)
        }, longPressTimeout);
    }

    // Cancel long press on key release
    document.addEventListener("keyup", function () {
        clearTimeout(longPressTimer);
    });
});
</script>
    
   

<script>
        getKaiAd({
	publisher: '58d21092-87f8-47c3-883d-fcd67eb318d8',
	app: '10',
	onerror: err => console.error('Custom catch:', err),
	onready: ad => {
		// Ad is ready to be displayed
		// calling 'display' will display the ad
		ad.call('display')
	}
})
    </script>



<script>
document.addEventListener("visibilitychange", function () {
    if (!document.hidden) {
        document.activeElement.blur();
        document.body.focus();
    }
});
</script>

<script>
        getKaiAd({
	publisher: '58d21092-87f8-47c3-883d-fcd67eb318d8',
	app: '10',
	onerror: err => console.error('Custom catch:', err),
	onready: ad => {
		// Ad is ready to be displayed
		// calling 'display' will display the ad
		ad.call('display')
	}
})
    </script>


<script>
        getKaiAd({
	publisher: '58d21092-87f8-47c3-883d-fcd67eb318d8',
	app: '10',
	onerror: err => console.error('Custom catch:', err),
	onready: ad => {
		// Ad is ready to be displayed
		// calling 'display' will display the ad
		ad.call('display')
	}
})
    </script>


<script>
        getKaiAd({
	publisher: '58d21092-87f8-47c3-883d-fcd67eb318d8',
	app: '10',
	onerror: err => console.error('Custom catch:', err),
	onready: ad => {
		// Ad is ready to be displayed
		// calling 'display' will display the ad
		ad.call('display')
	}
})
    </script>



<script>
        getKaiAd({
	publisher: '58d21092-87f8-47c3-883d-fcd67eb318d8',
	app: '10',
	onerror: err => console.error('Custom catch:', err),
	onready: ad => {
		// Ad is ready to be displayed
		// calling 'display' will display the ad
		ad.call('display')
	}
})
    </script>


<script>
document.addEventListener("keydown", function (event) {
  if (event.key === "SoftRight") {
    showExitPrompt();
  } else if (event.key === "ArrowLeft" || event.key === "ArrowRight") {
    toggleSelection();
  } else if (event.key === "Enter") {
    confirmExit();
  }
});

let exitPrompt = null;
let selectedButton = "no"; // Default selection

function showExitPrompt() {
  if (exitPrompt) return; // Prevent multiple prompts

  exitPrompt = document.createElement("div");
  Object.assign(exitPrompt.style, {
    position: "fixed",
    top: "50%",
    left: "50%",
    transform: "translate(-50%, -50%)",
    background: "#001f3f",
    color: "white",
    padding: "20px",
    textAlign: "center",
    borderRadius: "10px",
    zIndex: "1000",
    width: "220px",
    boxShadow: "0 0 10px rgba(255,255,255,0.3)"
  });

  let title = document.createElement("p");
  title.textContent = "Do you want to exit?";
  Object.assign(title.style, {
    fontSize: "18px",
    marginBottom: "15px",
    fontWeight: "bold"
  });
  exitPrompt.appendChild(title);

  let buttonContainer = document.createElement("div");
  Object.assign(buttonContainer.style, {
    display: "flex",
    justifyContent: "space-between"
  });

  let yesBtn = document.createElement("p");
  yesBtn.textContent = "✔ Yes";
  Object.assign(yesBtn.style, buttonStyle(false));
  yesBtn.id = "yesBtn";
  buttonContainer.appendChild(yesBtn);

  let noBtn = document.createElement("p");
  noBtn.textContent = "✖ No";
  Object.assign(noBtn.style, buttonStyle(true));
  noBtn.id = "noBtn";
  buttonContainer.appendChild(noBtn);

  exitPrompt.appendChild(buttonContainer);
  document.body.appendChild(exitPrompt);
}

function buttonStyle(selected) {
  return {
    padding: "10px 15px",
    borderRadius: "8px",
    cursor: "pointer",
    flex: "1",
    textAlign: "center",
    margin: "5px",
    fontSize: "16px",
    fontWeight: "bold",
    background: selected ? (selectedButton === "yes" ? "#2ecc71" : "#e74c3c") : "#34495e",
    color: "white",
  };
}

function toggleSelection() {
  if (!exitPrompt) return;

  let yesBtn = document.getElementById("yesBtn");
  let noBtn = document.getElementById("noBtn");

  if (selectedButton === "no") {
    selectedButton = "yes";
    Object.assign(yesBtn.style, buttonStyle(true));
    Object.assign(noBtn.style, buttonStyle(false));
  } else {
    selectedButton = "no";
    Object.assign(yesBtn.style, buttonStyle(false));
    Object.assign(noBtn.style, buttonStyle(true));
  }
}

function confirmExit() {
  if (!exitPrompt) return;

  if (selectedButton === "yes") {
    window.close(); // Attempts to close the app
  } else {
    exitPrompt.remove();
    exitPrompt = null;
  }
}
</script>


<script>
document.addEventListener('keydown', function (e) {
    if (e.key === '0') {
        e.preventDefault(); // Prevent default KaiOS behavior
        const cursorEnabled = document.body.classList.toggle('cursor-enabled');

        if (cursorEnabled) {
            alert('Mouse Cursor Enabled');
        } else {
            alert('Mouse Cursor Disabled');
        }
    }
});
</script>



<?php
// Set the path for your JSON file
$jsonFilePath = 'traffic_data.json';

// Get the referrer
$referrer = isset($_SERVER['HTTP_REFERER']) ? parse_url($_SERVER['HTTP_REFERER'], PHP_URL_HOST) : 'Direct Access';
$currentDate = date("Y-m-d");

// Load existing traffic data
if (file_exists($jsonFilePath)) {
    $existingData = json_decode(file_get_contents($jsonFilePath), true);
    if (!is_array($existingData)) {
        $existingData = []; // Initialize as an empty array if there was an issue
    }
} else {
    $existingData = []; // Create an empty array if file does not exist
}

// Check if today's date already exists in the data
if (!isset($existingData[$currentDate])) {
    $existingData[$currentDate] = []; // Initialize the date entry if it doesn't exist
}

// Check if the referrer already exists for today
if (isset($existingData[$currentDate][$referrer])) {
    // Increment visit count from this referrer for today
    $existingData[$currentDate][$referrer]['visits'] += 1;
} else {
    // Initialize visit count for this referrer for today
    $existingData[$currentDate][$referrer] = ['visits' => 1];
}

// Save updated data to JSON file
file_put_contents($jsonFilePath, json_encode($existingData, JSON_PRETTY_PRINT));

// Output success message (or you can simply redirect or show the website content)
echo "";
?>




</body>
</html>